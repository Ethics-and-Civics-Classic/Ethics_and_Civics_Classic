



interlink = {
	election_header = "GFX_faction_header_yellow"
	guiding_ethic = ethic_gestalt_consciousness
	use_guiding_ethic_as_pop_filter = no
	visual_ethic = "interlink_visual_ethic"

	unique = yes
	support_multiplier = 1

	resources = {
		category = pop_factions
		produces = {
			trigger = {
				owner = {
					NOT = { has_edict = civic_crowdsourcing_edict }
				}
			}
			unity = @faction_improved_output
		}
		produces = {
			trigger = {
				owner = {
					has_valid_civic = civic_media_conglomerate
					NOT = { has_edict = civic_crowdsourcing_edict }
				}
			}
			trade = @faction_improved_output
			mult = @media_conglomerate_multiplier
		}
		produces = {
			trigger = {
				owner = {
					has_edict = civic_crowdsourcing_edict
				}
			}
			physics_research = @faction_base_output
			engineering_research = @faction_major_output
			mult = @crowdsourcing_multiplier
		}
	}

	extortion_resource = {
		category = pop_factions
		produces = {
			trigger = {
				owner = {
					NOR = {
						has_civic = civic_crowdsourcing
						has_civic = civic_corporate_crowdsourcing
					}
				}
			}
			physics_research = @faction_base_output
			mult = @faction_extortion_research_ratio
		}
		produces = {
			trigger = {
				owner = {
					has_edict = civic_crowdsourcing_edict
				}
			}
			physics_research = @faction_major_output
			engineering_research = @faction_minor_output
			mult = @faction_extortion_crowdsourcing_ratio
		}
	}


	is_potential = {
		owner = {
			is_democratic_authority = yes
			has_country_flag = cyber_collectivist
		}
	}

	parameters = {
		empire = {
			type = country
			valid_objects = {
				is_same_value = root
			}
		}
	}

	can_join_pre_triggers = {
		is_enslaved = no
		is_being_purged = no
	}

	can_join_faction = {
		has_trait = trait_cybernetic
		can_think = yes
	}

	attraction = {
		base = 200
	}

	leader = {
		base = 100
	}

	demand = {
		title = "INTERLINK_NO_DEMANDS"
		unfulfilled_title = "INTERLINK_NO_DEMANDS"
		desc = "INTERLINK_NO_DEMANDS_DESC"

		fulfilled_effect = 0
		unfulfilled_effect = 0

		potential = {
			exists = owner
		}

		trigger = {
			always = yes
		}
	}

	on_create = {
	}

	on_destroy = {
	}

	actions = {

		extort_faction = {
			title = "EXTORT_FACTION"
			description = "EXTORT_FACTION_DESC"

			potential = {
				exists = owner
				owner = {
					is_oligarchic_authority = yes
					has_country_flag = cyber_collectivist
					NOT = { has_edict = civic_crowdsourcing_edict }
				}
				is_faction_extorted = no
			}

			effect = {
				set_faction_extorted = yes
				custom_tooltip = physics_research_extortion_tt
				custom_tooltip = ONLY_ONE_EXTORTED_FACTION_TT
			}

			ai_weight = {
				base = 0
			}
		}
		extort_faction_crowdsourcing = {
			title = "EXTORT_FACTION"
			description = "EXTORT_FACTION_DESC"

			potential = {
				exists = owner
				owner = {
					is_oligarchic_authority = yes
					has_country_flag = cyber_collectivist
					has_edict = civic_crowdsourcing_edict
				}
				is_faction_extorted = no
			}

			effect = {
				set_faction_extorted = yes
				custom_tooltip = crowdsourced_extortion_tt
				custom_tooltip = ONLY_ONE_EXTORTED_FACTION_TT
			}

			ai_weight = {
				base = 0
			}
		}
		cancel_extort_faction = {
			title = "CANCEL_EXTORT_FACTION"
			description = "CANCEL_EXTORT_FACTION_DESC"

			potential = {
				exists = owner
				owner = {
					is_oligarchic_authority = yes
					has_country_flag = cyber_collectivist
				}
				is_faction_extorted = yes
			}

			effect = {
				set_faction_extorted = no
			}

			ai_weight = {
				base = 0
			}
		}
	}
}
