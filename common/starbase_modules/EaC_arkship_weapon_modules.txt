# If you add a new weapon module, also add it to common/inline_scripts/arkship/ai_design.txt
# and update the weights for all arkship starbase types in common/starbase_types/00_starbase_types.txt

arkship_spinal_branch_office_mount = {
	icon = "GFX_ship_part_district_resort_secondary"
	section = "ARKSHIP_SPINAL_BRANCH_OFFICE"
	construction_days = 720
	starbase_type = arkship
	category = sm_spinal_cat

	potential = {
		is_arkship_starbase = yes
	}
	
	possible = {
		if = { 
			limit = { owner? = { has_origin = origin_forever_cruise } }
			owner? = {
				has_technology = tech_resort_colonies
			}
		}
		else = {
			custom_tooltip = {
				fail_text = must_be_cruise_arkship
				owner? = { has_origin = origin_forever_cruise }
			}
		}
	}

	show_in_tech = "tech_resort_colonies"

	show_tech_unlock_if = {
		is_nomadic = yes
	}

	fits_on_slots = { spinal_mount_1 }

	resources = {
		category = starbase_arkship_upgrades
		cost = {
			alloys = 2000
			consumer_goods = 1000
			unity = @arkship_cost_unity_500
			influence = 100
		}
	}
	
	triggered_country_modifier = {
		potential = {
			owner? = {
				NOT = { has_technology = tech_construction_templates }
			}
		}
		country_resource_max_add = 5000
		situation_nomad_economy_max_progress_add = 500
	}

	triggered_country_modifier = {
		potential = {
			owner? = {
				has_technology = tech_construction_templates
			}
		}
		country_resource_max_add = 10000
		situation_nomad_economy_max_progress_add = 1000
	}
	
	planet_modifier = {
		planet_max_districts_add = 2
	}
	
	station_modifier = {
		show_only_custom_tooltip = no
		arkship_harvest_resources_produces_mult = 0.15
	}

	component_set = forever_cruise_BRANCH_OFFICE
	show_component_tooltips = yes
	#custom_tooltip = sm_arkship_spinal_devastator_mount_tt

	ai_weight = {
		weight = 100 #use same weight here - ai weighting is done in starbase_types.txt
	}
}