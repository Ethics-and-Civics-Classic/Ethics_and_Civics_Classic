modifier = {
	factor = @cruise_contract_mult
}
modifier = {
	factor = 0
	has_country_flag = contract_conservation_study_$VARIANT$_issued
}
modifier = {
	factor = 0
	has_country_flag = conservation_study_failed
}
random_owned_fleet = {
	limit = {
		is_arkship_fleet = yes
	}
	solar_system = {
		save_event_target_as = arkship_system
	}
}
random_system = {
	limit = {
		distance = {
			source = event_target:arkship_system
			min_jumps = 1
			max_jumps = 20
		}
		root = {
			intel_level = {
				level > none
				system = prev
			}
		}
		OR = {
			is_conservation_site_space_fauna = yes
			any_system_planet = {
				OR = {
					is_conservation_site_default = yes
					is_conservation_site_wildlife = yes
				}
			}
		}
		NOT = {
			has_star_flag = conservation_study_contract_issued@root
		}
	}
	save_event_target_as = contract_system
	event_target:nomad_country = {
		issue_contract = {
			contract = contract_conservation_study_$VARIANT$
			location = event_target:contract_system
			target = root
		}
		set_country_flag = conservation_study_issued
		set_timed_country_flag = {
			flag = contract_conservation_study_$VARIANT$_issued
			days = 720
		}
	}
}
