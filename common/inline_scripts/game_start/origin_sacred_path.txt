if = {
	limit = {
		owner? = {
			has_origin = origin_sacred_path
		}
	}
	if = {
		limit = {
			owner = {
				is_gestalt = no
			}
		}
		add_zone = {
			district = district_ark_city
			zone = zone_ark_sacred_path_pilgrims
			zone_slot = 1
			replace = yes
		}
	}
	if = {
		limit = {
			owner = {
				is_green = yes
			}
		}
		if = {
			limit = {
				owner = {
					has_civic = civic_keepers_harmony
					NOT = { has_civic = civic_naturalism }
				}
			}
			add_building = {
				zone = zone_ark_sacred_path_pilgrims
				building = building_nature_sanctuary_nomad
			}
			add_blocker = { type = d_ark_nature_preserve_blocker }
		}
		else_if = {
			limit = {
				owner = {
					has_civic = civic_naturalism
					NOT = { has_civic = civic_keepers_harmony }
				}
			}
			add_building = {
				zone = zone_ark_sacred_path_pilgrims
				building = building_national_park_nomad
			}
			add_blocker = { type = d_ark_nature_preserve_blocker }
		}
		else_if = {
			limit = {
				owner = {
					has_civic = civic_naturalism
					has_civic = civic_keepers_harmony
				}
			}
			add_building = {
				zone = zone_ark_sacred_path_pilgrims
				building = building_nature_sanctuary_nomad
			}
			add_blocker = { type = d_ark_nature_preserve_blocker }
		}
		else = {
			add_building = {
				zone = zone_ark_sacred_path_pilgrims
				building = building_nomad_conservatory
			}
			add_blocker = { type = d_ark_nature_preserve_blocker }
		}
	}
}