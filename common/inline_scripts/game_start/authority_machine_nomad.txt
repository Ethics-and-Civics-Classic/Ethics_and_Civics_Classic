# Nomadic Machine Intelligence Initial Setup
# Urban Districts:
#	* 2× Ark City Districts
# Buildings Added:
#	* Administrative Array
#	* Machine Assembly Plant
#	* Drone Storage
# Zones:
#	* Collective Research Complex
#	* Military Logistics Complex / Industrial-Trade Quarter (Servitor / Obsessional Directive)
# Rural Districts are set to:
#	* 3× Generator Districts
#	* 4× Mining Districts
#	* 0× Agriculture Districts

if = {
	limit = {
		owner? = {
			is_machine_empire = yes
			is_nomadic = yes
		}
	}
	add_building = building_machine_capital
	add_building = building_machine_assembly_plant
	add_building = building_drone_storage

	add_district = district_ark_city
	add_district = district_ark_city

	add_zone = {
		district = district_ark_city
		zone = zone_ark_research_unity_gestalt
		zone_slot = 1
	}

	if = {
		limit = {
			owner = {
				OR = {
					has_valid_civic = civic_machine_servitor
					has_valid_civic = civic_machine_obsessional_directive
				}
			}
		}
		add_zone = {
			district = district_ark_city
			zone = zone_ark_industrial
			zone_slot = 2
		}
		add_building = {
			building = building_factory_1
			zone = zone_ark_industrial
		}
	}
	else = {
		add_zone = {
			district = district_ark_city
			zone = zone_ark_foundry
			zone_slot = 2
		}
	}
	add_building = {
		#district = district_city
		zone = zone_ark_research_unity_gestalt
		building = building_primitive_nuclear_plant
	}
	owner = { add_resource = { primitive_uranium = 1000 } }

	set_variable = {
		which = energy_districts
		value = 3
	}
	set_variable = {
		which = mining_districts
		value = 4
	}
	set_variable = {
		which = farming_districts
		value = 0
	}
}
