# Individualist Empire Initial Setup
# Urban Districts:
#	- 2× City Districts
# Buildings Added:
#	* Planetary Administration
# Zones:
#	* Archive Specialization
#	* Industrial Specialization

if = {
	limit = {
		exists = owner
		owner = {
			is_regular_empire = yes
			NOR = {
				is_eager_explorer_empire = yes
				has_origin = origin_primitive_player_early_space_age
			}
		}
	}
	add_building = building_capital

	add_district_and_planet_size_if_needed_effect_amount = {
		amount = 2
		district = district_city
	}

	add_zone = {
		district = district_city
		zone = zone_research_unity
		zone_slot = 1
	}
	add_zone = {
		district = district_city
		zone = zone_industrial
		zone_slot = 2
	}
	add_building = {
		building = building_factory_1
		zone = zone_industrial
	}
}
else_if = {
	limit = {
		exists = owner
		owner = {
			is_regular_empire = yes
			is_eager_explorer_empire = yes
		}
	}
	add_building = building_capital

	add_district_and_planet_size_if_needed_effect_amount = {
		amount = 2
		district = district_city
	}

	add_zone = {
		district = district_city
		zone = zone_urban
		zone_slot = 1
	}
	add_zone = {
		district = district_city
		zone = zone_industrial
		zone_slot = 2
	}
	add_building = {
		building = building_factory_1
		zone = zone_industrial
	}
}
else_if = {
	limit = {
		exists = owner
		owner = {
			is_regular_empire = yes
			has_origin = origin_primitive_player_early_space_age
		}
	}
	add_building = building_capital

	add_district_and_planet_size_if_needed_effect_amount = {
		amount = 2
		district = district_city
	}

	add_zone = {
		district = district_city
		zone = zone_trade	#Cog note - Trade Zone has helpful buildings that are only available with Trade Zone early on.
		zone_slot = 1
	}
	add_zone = {
		district = district_city
		zone = zone_urban
		zone_slot = 2	#Cog Note - Slot two cannot be changed until after Through the Ages, best to have this be Urban
	}
	add_building = {
		building = building_fallout_shelter
		zone = zone_urban
	}
}
