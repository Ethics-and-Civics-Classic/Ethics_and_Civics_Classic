triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			has_wrangler_job = yes
		}
	}
	job_wrangler_add = 0.20
	#job_wrangler_add = 1
	# mult = modifier:job_wrangler_or_variant_add	#Allowing this causes Linux CTD on start-up
}
triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			has_wrangler_job = yes
		}
	}
	job_wrangler_per_pop = 0.20
	#job_wrangler_per_pop = 1
	# mult = modifier:job_wrangler_or_variant_per_pop	#Allowing this causes Linux CTD on start-up
}

triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			has_rancher_job = yes
		}
	}
	job_rancher_add = 0.20
	#job_rancher_add = 1
	# mult = modifier:job_wrangler_or_variant_add	#Allowing this causes Linux CTD on start-up
}
triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			has_rancher_job = yes
		}
	}
	job_rancher_per_pop = 0.20
	#job_rancher_per_pop = 1
	# mult = modifier:job_wrangler_or_variant_per_pop	#Allowing this causes Linux CTD on start-up
}

triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			has_trophy_hunter_job = yes
		}
	}
	job_trophy_hunter_add = 0.20
	#job_trophy_hunter_add = 1
	# mult = modifier:job_wrangler_or_variant_add	#Allowing this causes Linux CTD on start-up
}
triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			has_trophy_hunter_job = yes
		}
	}
	job_trophy_hunter_per_pop = 0.20
	#job_trophy_hunter_per_pop = 1
	# mult = modifier:job_wrangler_or_variant_per_pop	#Allowing this causes Linux CTD on start-up
}

triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			has_animal_handler_job = yes
		}
	}
	job_animal_handler_add = 0.20
	#job_animal_handler_add = 1
	# mult = modifier:job_wrangler_or_variant_add	#Allowing this causes Linux CTD on start-up
}
triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			has_animal_handler_job = yes
		}
	}
	job_animal_handler_per_pop = 0.20
	#job_animal_handler_per_pop = 1
	# mult = modifier:job_wrangler_or_variant_per_pop	#Allowing this causes Linux CTD on start-up
}

triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			has_beastfighter_job = yes
		}
	}
	job_beastfighter_add = 0.20
	#job_beastfighter_add = 1
	# mult = modifier:job_wrangler_or_variant_add	#Allowing this causes Linux CTD on start-up
}
triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			has_beastfighter_job = yes
		}
	}
	job_beastfighter_per_pop = 0.20
	#job_beastfighter_per_pop = 1
	# mult = modifier:job_wrangler_or_variant_per_pop	#Allowing this causes Linux CTD on start-up
}	

triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			has_drone_wrangler_job = yes
		}
	}
	job_drone_wrangler_add = 0.20
	#job_drone_wrangler_add = 1
	# mult = modifier:job_wrangler_or_variant_add	#Allowing this causes Linux CTD on start-up
}
triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			has_drone_wrangler_job = yes
		}
	}
	job_drone_wrangler_per_pop = 0.20
	#job_drone_wrangler_per_pop = 1
	# mult = modifier:job_wrangler_or_variant_per_pop	#Allowing this causes Linux CTD on start-up
}

triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			has_hunter_drone_job = yes
		}
	}
	job_hunter_drone_add = 0.20
	#job_hunter_drone_add = 1
	# mult = modifier:job_wrangler_or_variant_add	#Allowing this causes Linux CTD on start-up
}
triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			has_hunter_drone_job = yes
		}
	}
	job_hunter_drone_per_pop = 0.20
	#job_hunter_drone_per_pop = 1
	# mult = modifier:job_wrangler_or_variant_per_pop	#Allowing this causes Linux CTD on start-up
}

triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			has_symbiosis_integrator_job = yes
		}
	}
	job_symbiosis_integrator_add = 0.20
	#job_symbiosis_integrator_add = 1
	# mult = modifier:job_wrangler_or_variant_add	#Allowing this causes Linux CTD on start-up
}
triggered_planet_modifier = {
	potential = {
		exists = owner
		owner = {
			has_symbiosis_integrator_job = yes
		}
	}
	job_symbiosis_integrator_per_pop = 0.20
	#job_symbiosis_integrator_per_pop = 1
	# mult = modifier:job_wrangler_or_variant_per_pop	#Allowing this causes Linux CTD on start-up
}
